Backbone artist!
<br>

<%= artist.escape("name") %>

<ul>
<% if (artist.artworks) { %>
  <% artist.artworks.each(function(artwork) { %>
  <li>
    <a href="<%= artwork.escape("image_url") %>"><%= artwork.escape("title") %></a>
  </li>
  <% }) %>
<% } %>
</ul>
