<% require_current_user(User.find(params[:user_id])) %>

<% @action = user_user_profile_url(current_user) %>
<% @method = 'patch' %>

<%= button_to "View Profile", "#users/#{current_user.id}", method: :get %>
<%= render 'form' %>
