<header class="group">
  <%= image_tag("logo-small.png", alt: "logo") %>
  <br>

  <% if logged_in? %>
    <%= current_user.username %>
    <%= button_to "Sign Out", session_url, method: :delete %>
  <% end %>



</header>