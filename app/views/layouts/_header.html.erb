<header class="group">
  <img src="<%= asset_path 'logo-small.png' %>"></img>
  <br>

  <% if logged_in? %>
    <%= current_user.username %>
    <%= button_to "Sign Out", session_url(current_user), method: :delete %>
    <%= button_to "Add Art", new_artwork_url, method: :get %>
  <% end %>



</header>